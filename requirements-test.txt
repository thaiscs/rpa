# Test dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0

# HTTP client for testing
httpx>=0.24.0

# Async file operations
aiofiles>=23.0.0

# Message queue
pika>=1.3.0       # used by api/services/queue_service.py (sync client)
aio-pika>=9.0.0   # used by worker/worker.py (async client)

# UI framework (needed by ui helper tests)
nicegui>=1.4.0
